# リポジトリの手順

## 言語とドキュメント

- ソースコード、設定例、コミットメッセージ、ソースドキュメントには英語を使用します。
- `README.md` と `AGENTS.md` がソースドキュメントです。
- いずれかのソースドキュメントを変更した後、`mdt --lang ja --force <file>` で `README.ja.md` と `AGENTS.ja.md` を生成します。
- 生成された日本語ドキュメントを英語ソースと同期させておきます。

## スコープ

- プロジェクトが明示的にプラットフォーム範囲を拡大するまで、macOS のみをサポートします。
- デスクトップ UI には GPUI を使用し、ネイティブのタイトルバーとリサイズ可能なウィンドウ動作を維持します。
- ストリーミングには Rig の OpenAI Chat Completions クライアントを使用し、カスタムベース URL もサポートします。
- OpenAI 互換の契約で十分な場合、ローカル推論ランタイム、llama 統合、プロバイダー固有の SDK は追加しないでください。
- トークンは `config.toml` とは別に保管します。現在のプレーンテキスト形式の `credentials.toml` は意図的なものです。実際の認証情報をコミットしないでください。

## アーキテクチャの境界

- GPUI エンティティは GPUI アプリケーションスレッド上でのみ変更します。
- ネットワーク操作は専用の Tokio ランタイムスレッド上で実行します。
- コールバック、UI、ネットワークの境界をまたぐ通信には有界チャネルを使用します。
- すべてのストリーミングイベントにリクエスト ID を添付し、古いイベントは無視します。
- 新しい翻訳を開始する前に、アクティブなストリームをキャンセルします。
- Accessibility へのアクセスは `selection.rs`、プロバイダーのストリーミングは `llm.rs`、UI レンダリングは `ui.rs` に保持します。
- `unsafe` コードは避けます。macOS API には安全なラッパーを優先します。

## 設定の互換性

- 設定ルートは `xdg::BaseDirectories` を通じて解決します。デフォルトは `~/.config/translate-popup` で、`XDG_CONFIG_HOME` を尊重します。
- 名前付きプロバイダーと認証情報の対応関係を維持します。
- 各設定済みグローバルショートカットに付随する対象言語を、ホットキー、UI、ネットワークの各境界をまたいで維持します。
- ショートカット文字列、TOML コンテンツ、Accessibility の値、HTTP レスポンスは信頼できない境界入力として扱います。
- `credentials.toml` のパーミッションを `0600` に保ちます。

## 開発コマンド

リポジトリルートから実行します:

```bash
just fix
just fix-format
just fix-lint
just check
just check-format
just check-lint
just test
just build
just ci
```

`just ci` を使用してローカルの完全な検証ゲートを実行します。反復作業中は最小の関連する `check-*` または `fix-*` レシピを使用し、引き渡し前に完全なゲートを実行します。失敗しているテストやリントを弱めてはいけません。

## 自動化

- リポジトリの自動化は、デフォルトでルートの `Justfile` に保持します。
- スタンドアロンのシェルスクリプトよりも、名前付きの Just レシピを優先します。
- Just で合理的に表現できないワークフローの場合のみスクリプトを追加し、そのスクリプトを Just レシピから呼び出します。

## 変更の規律

- 可能な場合、本番ソースファイルは 250 行未満に保ちます。
- 設定検証、プロンプト契約、キャンセル、または古いイベントの動作の境界が変更された場合は、それらのテストを追加します。
- API キーをログに出力したり表示したりしないでください。
- ユーザーが明示的に GitHub 作業を要求しない限り、Git リモートの変更、ブランチのプッシュ、プルリクエストの作成は行いません。