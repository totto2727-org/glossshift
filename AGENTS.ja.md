# リポジトリの指示

## 言語とドキュメント

- ソースコード、設定例、コミットメッセージ、ソースドキュメントには英語を使用する。
- `README.md` と `AGENTS.md` がソースドキュメントである。
- いずれかのソースドキュメントを変更した後、`mdt --lang ja --force <file>` で `README.ja.md` と `AGENTS.ja.md` を生成する。
- 生成された日本語ドキュメントを英語ソースと同期させておく。

## スコープ

- プロジェクトが明示的にプラットフォームの範囲を拡張するまで、macOS のみをサポートする。
- デスクトップ UI には GPUI を使用し、ネイティブのタイトルバーとリサイズ可能なウィンドウの動作を維持する。
- カスタムベース URL を含むストリーミングには、Rig の OpenAI Chat Completions クライアントを使用する。
- OpenAI 互換の契約で十分な場合、ローカル推論ランタイム、llama 統合、プロバイダー固有の SDK を追加しない。
- トークンを `config.toml` から分離しておく。現在のプレーンテキスト `credentials.toml` は意図的なものであり、実際の認証情報をコミットしないこと。

## アーキテクチャの境界

- GPUI エンティティは GPUI アプリケーションスレッドでのみ変更する。
- ネットワーク操作は専用の Tokio ランタイムスレッドで実行する。
- コールバック、UI、ネットワークの境界をまたぐ際は bounded channel を使用する。
- すべてのストリーミングイベントにリクエスト ID を付け、古いイベントを無視する。
- 新しい翻訳を開始する前に、アクティブなストリームをキャンセルする。
- Accessibility アクセスを `selection.rs`、プロバイダーのストリーミングを `llm.rs`、UI レンダリングを `ui.rs` に保持する。
- XDG 設定、プロンプト構築、プロバイダーのストリーミングを、両バイナリが使用する共有ライブラリに保持する。
- CLI のパス解決と ANSI Markdown レンダリングを `cli.rs` に保持し、自動カラー モードでは生成ファイルやリダイレクトされた標準出力に ANSI エスケープを書き込まない。
- 両バイナリをパッケージの派生に含め、対応する `meta.mainProgram` を持つデフォルトの flake オーバーレイを通じて `translate-popup` と `translate-popup-cli` を公開する。
- `unsafe` コードを避ける。macOS API には安全なラッパーを優先する。

## 設定の互換性

- `xdg::BaseDirectories` を通じて設定ルートを解決する。デフォルトは `~/.config/translate-popup` で、`XDG_CONFIG_HOME` を尊重する。
- 名前付きプロバイダーと認証情報の関係を維持する。
- 設定された各グローバルショートカットに付随するターゲット言語を、ホットキー、UI、ネットワークの境界をまたいで維持する。
- ショートカット文字列、TOML コンテンツ、Accessibility の値、HTTP レスポンスを信頼できない境界入力として扱う。
- `credentials.toml` のパーミッションを `0600` に保つ。

## 開発コマンド

リポジトリのルートから実行する：

```bash
just fix
just fix-format
just fix-lint
just check
just check-format
just check-lint
just test
just build
just ci
just cli README.md --lang ja
```

完全なローカル検証ゲートには `just ci` を使用する。反復作業中は最も小さな関連する `check-*` または `fix-*` レシピを使用し、引き渡しの前に完全なゲートを実行する。失敗しているテストや lint を弱めないこと。

## 自動化

- リポジトリの自動化は、デフォルトではルートの `Justfile` に保持する。
- スタンドアロンのシェルスクリプトよりも、名前付きの Just レシピを優先する。
- ワークフローが Just で合理的に表現できない場合のみスクリプトを追加し、そのスクリプトを Just レシピから呼び出す。

## 変更規律

- 可能な場合は、本番ソースファイルを 250 行未満に保つ。
- 設定検証、プロンプト契約、キャンセル、または古いイベントの動作に関連する境界を変更するときは、テストを追加する。
- API キーをログに記録したりレンダリングしたりしない。
- ユーザーが明示的に GitHub の作業を要求しない限り、Git リモートを変更したり、ブランチをプッシュしたり、プルリクエストを作成したりしない。