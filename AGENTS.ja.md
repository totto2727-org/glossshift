# リポジトリの指示

## 言語とドキュメント

- ソースコード、設定例、コミットメッセージ、ソースドキュメントには英語を使用してください。
- `README.md` と `AGENTS.md` がソースドキュメントです。
- いずれかのソースドキュメントを変更した後、`mdt --lang ja --force <file>` で `README.ja.md` と `AGENTS.ja.md` を生成してください。
- 生成された日本語ドキュメントは英語版ソースと同期を保ってください。

## スコープ

- プロジェクトが明示的にプラットフォームの範囲を拡張するまで、macOS のみをサポートします。
- デスクトップ UI には GPUI を使用し、ネイティブのタイトルバーとリサイズ可能なウィンドウ動作を維持してください。
- ストリーミングには Rig の OpenAI Chat Completions クライアントを使用し、カスタムベース URL にも対応してください。
- OpenAI 互換の契約で十分な場合は、ローカル推論ランタイム、llama 統合、プロバイダ固有 SDK を追加しないでください。
- トークンは `config.toml` とは別に保管してください。現在の平文 `credentials.toml` は意図的なものです。実際の認証情報をコミットしないでください。

## アーキテクチャの境界

- GPUI エンティティは GPUI アプリケーションスレッド上でのみ変更してください。
- ネットワーク操作は専用の Tokio ランタイムスレッドで実行してください。
- コールバック、UI、ネットワークの境界には境界付きチャネルを使用してください。
- すべてのストリーミングイベントにリクエスト ID を付与し、古いイベントは無視してください。
- 新しい翻訳を開始する前に、アクティブなストリームをキャンセルしてください。
- Accessibility へのアクセスは `selection.rs`、プロバイダのストリーミングは `llm.rs`、UI レンダリングは `ui.rs` に維持してください。
- XDG 設定、プロンプト構築、プロバイダのストリーミングは、両バイナリで共有されるライブラリに維持してください。
- CLI のパス解決と ANSI Markdown レンダリングは `cli.rs` に維持し、自動カラーモードでは ANSI エスケープを生成ファイルやリダイレクトされた標準出力に書き込まないでください。
- 両方のバイナリをパッケージ派生に含め、デフォルトの flake overlay で一致する `meta.mainProgram` を持つ `glossshift` と `gshift` を公開してください。
- `unsafe` コードを避けてください。macOS API の安全なラッパーを優先してください。

## 設定の互換性

- 設定ルートは `xdg::BaseDirectories` を通じて解決してください。デフォルトは `~/.config/glossshift` で、`XDG_CONFIG_HOME` を尊重します。
- 名前付きプロバイダと認証情報の関係を維持してください。
- 設定された各グローバルショートカットに付随するターゲット言語を、ホットキー、UI、ネットワークの各境界を越えて維持してください。
- ショートカット文字列、TOML コンテンツ、Accessibility の値、HTTP レスポンスは、信頼できない境界入力として扱ってください。
- `credentials.toml` の権限を `0600` に保ってください。

## 開発コマンド

リポジトリのルートから実行してください:

```bash
just fix
just fix-format
just fix-lint
just check
just check-format
just check-lint
just test
just build
just ci
just cli README.md --lang ja
```

フルローカルの検証ゲートには `just ci` を使用してください。反復作業中は最小の関連する `check-*` または `fix-*` レシピを使用し、引き渡し前にフルゲートを実行してください。失敗しているテストや lint を弱めてはいけません。

## 自動化

- リポジトリの自動化はデフォルトでルートの `Justfile` に保持してください。
- スタンドアロンのシェルスクリプトよりも、名前付きの Just レシピを優先してください。
- ワークフローを Just で合理的に表現できない場合にのみスクリプトを追加し、そのスクリプトは Just レシピから呼び出してください。

## 変更の規律

- 可能な場合は、本番ソースファイルを 250 行未満に保ってください。
- 設定検証、プロンプト契約、キャンセル、または古いイベントの動作の境界が変更された場合は、それらのテストを追加してください。
- API キーをログに出力したり表示したりしないでください。
- ユーザーが明示的に GitHub の作業を依頼しない限り、Git リモートの変更、ブランチのプッシュ、プルリクエストの作成は行わないでください。